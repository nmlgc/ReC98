// Set to `true` to quit out of the main loop. Used to indicate either a Game
// Over or a request to quit to the main menu (which are treated identically
// in this game), or the end of a demo replay.
extern bool quit;

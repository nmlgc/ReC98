#if (GAME == 5)
// Renaming the function on this branch makes sure that we remember this
// essential change in the future when we merge the decompilation.
void near staffroll_animate_with_verdict_bitmap_on_heap(void);
#else
void near staffroll_animate(void);
#endif
